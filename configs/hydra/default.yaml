# https://hydra.cc/docs/configure_hydra/intro/

# enable color logging
defaults:
  - override hydra_logging: colorlog
  - override job_logging: colorlog

# output directory, generated dynamically on each run
run:
  dir: ${paths.log_dir}/${task_name}/runs/${now:%Y-%m-%d}_${now:%H-%M-%S}
sweep:
  dir: ${paths.log_dir}/${task_name}/multiruns/${now:%Y-%m-%d}_${now:%H-%M-%S}
  subdir: ${hydra.job.num}

job_logging:
  handlers:
    file:
      # Incorporates fix from https://github.com/facebookresearch/hydra/pull/2242
      filename: ${hydra.runtime.output_dir}/${task_name}.log

  formatters:
    simple:
      # Use the custom formatter class we created
      "()": src.utils.pylogger.RelativePathFormatter
      # Use the new 'relativePath' attribute in the format string
      format: "[%(asctime)s][%(relativePath)s:%(lineno)d][%(levelname)s] - %(message)s"
    colorlog:
      # Use the custom formatter class we created
      "()": src.utils.pylogger.RelativePathFormatter
      # Use the new 'relativePath' attribute in the format string
      format: "[%(cyan)s%(asctime)s%(reset)s][%(blue)s%(relativePath)s%(reset)s][%(log_color)s%(levelname)s%(reset)s] - %(message)s"
